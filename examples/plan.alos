module main;

# imports folder/file.alos
use "folder/file";

let a = "Random string";
fixed b = "Random string";

# Builtins get called with @
@println(a, b);

sub blockSub(a, b) -> {
  return 1;
}

sub arrowSub(a, b) -> 1;