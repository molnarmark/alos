module main;

# imports folder/file.alos
use "folder/file";

let a = "Random string";
fixed b = "Random string";

# Builtins get called with @
@println("Test");

sub something(a, b, c) -> {
  return 1;
}

sub something(a, b, c) -> 1;